[build]
builder = "DOCKERFILE"
dockerfilePath = "formguard-near/mock-near/Dockerfile"

[deploy]
startCommand = "gunicorn -w 2 -b 0.0.0.0:$PORT server:app"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
